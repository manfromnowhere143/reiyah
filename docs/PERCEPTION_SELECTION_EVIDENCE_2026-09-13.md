# Selection evidence boundary, 2026-09-13

Document ID: `reiyah.selection-evidence.checkpoint`; version `0.1.0`; status `exploratory`.

Use the existing discovery locator and retain selection custody separately. The checked
development selection fits that interface without changing its capture or original indices,
so this checkpoint adds a coordinator procedure and one boundary regression, with no new
production converter, schema or admission behavior. Read the
[procedure](../research/perception-selection-evidence/0.1.0/README.md) and
[verification](../research/perception-selection-evidence/0.1.0/verification.json).

The new synthetic check preserves a large valid viewer selection while the existing discovery
limit refuses it without truncation. An unreturned, unsuccessful or ambiguous interaction is
not a zero, an empty object set or a confident class. The previously prepared development
exercise is exposed material and asks for no object interpretation; even a successful return
does not qualify as independent discovery. No actual participant record or proposal was created.

One new test passes. The earlier 379 repository tests and 93 measurement tests are retained
against unchanged source, not presented as new runs. Source identity and direct original
record slices are rechecked. Background/code conformance remains distinct from actual
interactive observation, human usability and independent scientific review.

The next question is whether compiler representation duplicates stable objects across joint
alternatives until an otherwise small comparison exceeds its graph budget. Establish the
deficiency, direct per-world comparator, assumptions, measured cost and falsifier before changing
code. A resource-induced open bound is conservative; do not misdescribe it as a false finite
answer. No Fable planner, comparator, source, ref, status or outbox is part of that Engine task.

Real bounds remain [-8,8]; human references and external scientific review are missing. Gate A
is unaccepted; the physical study is unrun with no selected cohort or seed. Fable's selected
c70e6ce negative-report review remains scoped and held as documented in the previous checkpoint.
P005 is operator-reported published; no further publication or outreach. Preserve the closed
Engine packets and every owner checkout, and continue the authorized autonomous window.
