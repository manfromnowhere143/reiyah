# Reiyah research continuation and stopping point

Document ID: `reiyah.research-continuation.2026-09-07`

Version: `0.1.0`

Lifecycle status: `exploratory`

The operator requested autonomous research and implementation and a read-only review of
Sentinel, Telos and Inbar for useful lessons. An earlier shutdown window was extended and
then explicitly canceled: the operator subsequently said the machine would remain open for
the next 24 hours. The earlier approximate 08:44 UTC stopping target is superseded. Work
continues with durable checkpoints; completed evidence never depends on a surviving process.

## Exact research lineage and workspace boundary

The canonical Reiyah session root remains `/Users/danielwahnich/workspace/reiyah`, on the
separately owned Gate A branch. Do not switch it. The separate Gate B measurement worktree
also remains untouched. Read their current states before any future integration.

| Research stage | Local commit or topic | Scope |
| --- | --- | --- |
| Research board and AO | `4deb1d19ef1db6fa37fb6b20ca0b4ffda93c4bcc` | System reconstruction, primary research, 25-part verdict, reference correction and two validator repairs |
| Frozen reference study | `9d8a18eab7144bd02b1f64c41bc8f179f32ee653` | Sampling, raw evidence retrieval, 240 blinded packets, unknown-preserving analysis |
| Developer value and sibling review | `beccb6b50babfe9b203b8b8e4aad59c304003a89` | Selected sibling recalculations, current interface research, portable population auditor and continuation ledger |
| Cache selection and reference-noise interpretation | `research/2026-09-07-selection-policy` | Raw-metadata selection reconstruction, exact noise counterexample and updated stopping point |

The final local commit identity for the last row is recorded by the private `delivery.json`
and can be resolved from the branch. It must not be inferred from a remembered hash or a
working directory name. All research commits are authored and committed by Daniel Wahnich
alone. No merge, push, remote publication or external outreach is implied.

Private task custody is under:

```text
/Users/danielwahnich/.codex/reports/reiyah/
  research-board-2026-09-07/
  reference-study-2026-09-07/
  developer-value-2026-09-07/
  selection-policy-2026-09-07/
```

The last directory contains an exported `baseline/` at `beccb6b`, the reviewable
`implementation/`, retained source pointers and private payloads, checks, an exact candidate
delta, a patch and the final Git readback. These exports are not replacements for the canonical
repository identity gate. A new session must begin at the canonical Reiyah root and inspect
the local research ref without overwriting the active owners' work.

## Task ledger

| ID | Investigation or implementation | State | Next discriminating action |
| --- | --- | --- | --- |
| R01 | Reconstruct Reiyah and assess its scientific identity against primary research | Completed for the inspected snapshots | Reopen only for material new engine/UI evidence; use the board report's stated limits |
| R02 | Validate the reference behind unmatched-detection physical interpretations | Prepared; human observations pending | Obtain two independent blinded human reviews per frozen case; retain uncertainty and disagreement |
| R03 | Reproduce and narrow AO's reference-population correction | Completed in the board revision | Confirm through independent adjudication before making physical-existence claims |
| R04 | Learn selectively from Sentinel, Telos and Inbar | Bounded review and arithmetic audit completed | Apply the named controls to prospective Reiyah experiments; no codebase merge |
| R05 | Make a reference-population counterexample portable | Implemented and replayed on retained data | Use the demo, real-data report and verification record; distinguish the frame universes |
| R06 | Test external engineer value | Proposed; no participants contacted | Evaluate one independently selected pipeline against a competent baseline with equal information |
| R07 | Add MCP/API access | Deferred pending demonstrated workflow value | Pin current protocol and test actual clients only when an integration is justified |
| R08 | Investigate predictive joint-miss monitoring or intervention benefit | Conditional on reference adequacy | Freeze prospective populations, input-only baselines, temporal controls, false-alarm costs and scene transfer |
| R09 | Check whether an object-row cache omits frame opportunities | Completed retrospective sensitivity | Keep the complete frame census separate from object reference eligibility in future adapters |
| R10 | Reconstruct the cache's declared inclusion policy from raw metadata and upstream sources | Completed; exact membership and checked fields agree | Preserve the scoped result; this does not establish physical completeness or full SDK conformance |
| R11 | Test the external vision review's inference from M4 to general reference-error immunity | Broad interpretation corrected by an exact counterexample | Model reference transitions and opportunity selection explicitly before interpreting an error budget |
| R12 | Determine whether numerical M4 extrema are valid outer bounds and distinguish undefined from unbounded | Next bounded investigation | Construct analytic counterexamples before proposing a replacement solver |

R02 remains the highest-value scientific dependency. R04-R07 supplement it; they do not
replace missing judgments with more software. No human or model was impersonated as an
independent reviewer. The pending user clarification asks who can provide the two human
reviews. Permission to continue research does not answer that factual question.

## Resume the prepared reference study

Read [the study](REFERENCE_ADJUDICATION_STUDY_2026-09-07.md),
[its closeout](REFERENCE_STUDY_CLOSEOUT_2026-09-07.md), and
[reviewer instructions](../research/reference-study/0.2.0/REVIEWER_INSTRUCTIONS.md).
The study protocol is frozen at digest
`d78641db57f99e964414559b8646a19c5f38d902392ec17fd855d73d4f518ff6`.
Its exact selection digest is
`327cb685db2ffcc206ba9da6e0213c81e73c2926db7ba18b8cc651a41184612e`.

The private review entry point is:

```text
reference-study-2026-09-07/private/review-packets-v2/index.html
```

It links the 240 cases across 93 scenes. The 4,284 original sensor assets and the
per-file retrieval inventory are retained beside the review packets. The selection key,
unblinded mappings and case details must remain separate from reviewer access. Giving both
reviewers the entire private directory would compromise the intended blinding.

There are zero human judgments at this checkpoint. Every group's current support interval
is [0, 1], and physical-performance estimates are null. Distinct reviewer IDs alone do not
prove independent people or independent judgment. A new third-review procedure, changed
cohort, changed sampling rule, or different endpoint needs a new prospective protocol, not
an edit to the frozen study.

The first design, version 0.1.0, remains retained as a failed precision design. Do not revive
it from an older handoff. The active version 0.2.0 is a finite-population random sample of
60 detections in each of four strata; it makes no unseen-scene guarantee.

## Resume the portable audit and external-value investigation

Read [the portable tool](REFERENCE_AUDIT_DEMO_2026-09-07.md),
[the completed replay and frame audit](REFERENCE_AUDIT_REAL_DATA_2026-09-07.md),
[the developer-access decision](DEVELOPER_ACCESS_RESEARCH_2026-09-07.md), and
[the sibling review](SIBLING_RESEARCH_TRANSFER_2026-09-07.md).

The new tool has no model, network dependency, service process or write-capable remote API.
Its companion real-data adapter reads only private copies of the four AO/study inputs and
uses annotation tokens to check that cached positions belong to the wider supplied table.
Per-frame bundles and reports remain private because they contain third-party data and
opaque sample identifiers. The public record carries aggregate outcomes and digests only.

Only a completed `private/real-replay-1/result.json` plus a successful supervisor capture
supports a completed real-data replay. A `spec.json` or partially written JSONL file does not.
If interruption leaves no completion record, retain the partial directory as incomplete and
use a new run identity. Do not append unobserved outcomes or overwrite a prior run.

At this checkpoint both `private/real-replay-1/` and `private/frame-coverage-1/` have successful
completion records and retained process streams. The independently written spatial-index
comparison agrees on all 222,902 prediction classifications. This is numerical agreement on
shared source inputs, not independent annotation truth. The cache-conditioned replay covers
5,953 frames; the separate census contains 6,019 frames, leaving 66 in the sensitivity analysis.
All those local calculation processes have finished. See
[the current closeout](DEVELOPER_VALUE_CLOSEOUT_2026-09-07.md) for replay commands and bounds.

The current MCP comparison is pinned to protocol revision **2026-07-28**, whose source bytes
are retained. Older retrieved 2025 pages remain historical comparisons; they are not the
current interface recommendation. FiftyOne already documents MCP-assisted evaluation.
No novelty or performance advantage is claimed for wrapping a tool in that protocol.

## Latest completed checks and interpretation correction

Read [the latest checkpoint](SELECTION_POLICY_CLOSEOUT_2026-09-07.md) and
[cache-policy result](CACHE_SELECTION_AUDIT_2026-09-07.md). The raw-metadata reconstruction
compares all 134,565 selected annotation IDs and the named metadata fields, with zero
disagreements. The official validation census independently reconstructed from the retained
split has 6,019 frames, matching both prediction files. This new check closes the earlier
official-split limitation for these exact inputs; the earlier replay records remain unchanged.

Read [the reference-noise correction](REFERENCE_NOISE_INTERPRETATION_2026-09-07.md) before
using the older M4 interpretation or the operator-supplied model vision as a roadmap. M4's
rescaling theorem is true; its broad immunity reading is not. Shared, uniformly applied
reference-label corruption can create measured dependence between independent observers.
The exact construction is synthetic and does not identify the real data's noise mechanism.

The prepared 240-case detection study does not identify a complete opportunity census of
true joint misses or a safety-critical reference transition model. Preserve its original
question and frozen sample. A broader identification study requires its own prospective
population, observations and protocol. Do not sell or describe the current packets as a
reference certificate. The [vision disposition](VISION_REVIEW_2026-09-07.md) records the
useful commercial hypotheses and the unsupported transitions to authority or adoption.

## Cloud, checks and authority

The reference study's temporary CPU reader and cloned data disk were deleted, with readback
recorded in its cloud closeout. Its original GPU VM remained stopped and its original dataset
disk remained attached. No additional cloud resource was created for the developer-value work.
Do not restart cloud infrastructure simply to resume these local calculations.

The [developer-value verification record](../evidence/developer-value/verification-0.1.0.json)
binds the executed new tests, schema checks, demo replay and selected-source reaggregation.
The inherited study/board checks remain evidence for their exact earlier scope. Passing a
development checker does not freshly replay all 52 historical experiments or create a new
Gate A release.

Existing Gate A protocols, release operands, fixture catalogs, replay manifests and the
scientific claim register are unchanged by this revision. Operator acceptance remains
unaccepted. K129's prior contract replay is not implementation, an independent review or
empirical authority. No physical performance, frontier ranking, commercial demand, runtime
safety or publication acceptance is inferred from these changes.

The single next laboratory choice remains independent reference adjudication. It resolves
whether the physical interpretation has support; neither a merger nor an API endpoint can
answer that question.
