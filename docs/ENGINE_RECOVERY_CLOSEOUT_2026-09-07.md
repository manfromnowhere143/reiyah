# Recovered engine checkpoint

Document ID: `reiyah.engine-recovery-closeout.2026-09-07`

Version: `0.1.0`

Lifecycle status: `exploratory`

The operator explicitly requested takeover of the interrupted engine session,
continuation of its research, professional integration onto main when checks
support it, and preservation of the two separate UI/UX sessions. The canonical
identity preflight passed. The Gate A checkout and separate Gate B worktree
were neither switched nor edited.

## Exact stopping point and inherited work

The prior committed research tip was
`6925e7e114246c558a7905f1dbe7b0a393068c7d`. Its private continuation lived at
`~/.codex/reports/reiyah/predictive-monitor-2026-09-07/`. It contained seventeen
new source, test and plan files, completed scene/log/spatial fits, a separate
coefficient sensitivity and a training-log preflight. Its last successful
process was the corrected training preflight, ending 2026-09-07 at
14:04:33 UTC. No unfinished fit needed to be resumed.

The initial data builder rejected an unsupported operating-point representation;
its successor data specification and successful build remain distinct. The
first training preflight could not parse the retained SDK's computed train
union; its corrected AST parser and successful second run are retained. Neither
failed attempt is relabeled as successful evidence.

Recovery custody is `~/.codex/reports/reiyah/engine-recovery-2026-09-07/`.
`private/inherited-state.json` hashes the inherited candidate and records the
original owners' refs and clean status. `implementation/` is this reviewable
candidate; `baseline/` is the preceding research export. Start every future
repository task at the canonical Reiyah root and inspect its actual branch;
these exports do not replace the repository identity gate.

## Retained measurements and verification

Read the [findings](PREDICTIVE_MONITOR_FINDINGS_2026-09-07.md). Aggregate original
results are under `evidence/predictive-monitor/`; all opaque frame/scene maps,
prediction rows and raw third-party inputs remain private. The
[verification record](../evidence/predictive-monitor/verification-0.1.0.json)
binds completed commands, output digests, source identities and attack outcomes.
The successor claim register is
[`0.2.25`](../evidence/claim-status-register-2026-09-07T150115Z.json). It carries
every predecessor claim unchanged and adds four scoped interpretations.

The completion auditor passes on all three original runs. Its two executed
counterexamples prove that the older checker accepted an empty experiment set
and a forged spatial log-resampled band; the successor rejects both without
writing a success artifact. The older checker and every source-bound producer
remain unchanged. This is a computational integrity correction, not independent
human validation.

Fresh log and spatial refits reproduce every private prediction, fold and
geometry artifact byte for byte. Full report bytes differ: the producers embed
elapsed fitting and prediction times. The comparison retains every changed
JSON path and verifies that only those timing fields differ. It does not call
the full reports byte-identical or normalize away numerical differences.
The original scene fits were reaggregated, not refitted during this recovery.
The metadata training preflight reproduces its aggregate and split bytes.

The 46 research tests and 93 inherited tool regressions pass. The Gate B
development check passes, including register, custody and document checks. Its
record retains the actual scope. Historical replay rows remain `not_replayed_here` in this new development
check. The separately inherited main commit retains its historical full-replay
report; that report concerns its original bytes and is not a fresh replay of
this research candidate. Gate A release validation is not claimed by these
Gate B checks, and no frozen Gate A release operand or validator is changed.

To reaggregate the inherited run in the pinned analysis environment:

```sh
python -B tools/measure/audit_predictive_monitor_completion.py \
  --run-dir /path/to/private/log-evaluation-1 \
  --dataset /path/to/private/data-build-2/dataset.private.npz \
  --output /new/output/identity.json
python -B -m unittest discover -s tests -p 'test_*.py' -v
python -B -m unittest discover -s tools/measure -p 'test_*.py' -v
python -B tools/measure/gate_b_check.py --json /new/check/identity.json
```

The retained analysis specs pin NumPy 2.4.2 and scikit-learn 1.8.0. Producer
commands, input paths and original process captures remain in the private task
directories; their result fields must not be guessed from this example.

## Integration and next action

At takeover the observed remote main was
`9464ff79a823a91037604bb8343e984b29c128a5`; local main was an older Gate A ref.
The research line and remote main merge cleanly. The only main-only delta is
its historical full-replay report and the corresponding handoff entry. Preserve
both in integration, together with all existing research corrections. Exact
commit, merge, push and readback outcomes belong in the task's private delivery
record and Git topology; this document does not anticipate a completed push.

The [next training experiment](TRAINING_OVERLAP_NEXT_EXPERIMENT_2026-09-07.md)
separates training overlap from dataset transfer. Its immediate next action is
a sensor-file/model-provenance inventory and a fixed compute/calibration plan.
Metadata partitions alone cannot resolve that experiment. No new detector
training, cloud resource or second-dataset payload was started here.

No independent human reviewers are available at this checkpoint; the frozen
240-case study still has zero judgments. Operator acceptance of Gate A remains
`unaccepted`. Architecture completeness, computational replay, Git integration,
scientific support, safety and publication acceptance remain separate. The
stopped feature expansion and current reference-relative results do not justify
a breakthrough announcement. Preserve the negative findings and resume the
discriminating experiment, not post-hoc feature tuning.
