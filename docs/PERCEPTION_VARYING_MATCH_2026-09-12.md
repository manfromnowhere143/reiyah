# Common matching conformance checkpoint, 12 September 2026

Keep the current Engine interface. The selected constructed research case is
representable through the existing exact geometric compiler and common-input
projection, preserving all four interpretations and their different matching
competition. The [realization and replay](../research/perception-varying-match/0.1.0/README.md)
and [verification](../research/perception-varying-match/0.1.0/verification.json)
retain the evidence.

The deficiency tested was specific: aggregate interval agreement cannot show that
both analysts received the same interpretation-dependent decision. The bounded
alternative to an interface extension was one geometric realization plus a regression
using an existing exhaustive position-based baseline. Its assumptions are explicit
synthetic points, four joint worlds, one unit-weight anchor, unit FN/FP and tolerance
1/10. The falsifier was a changed world graph or loss after projection, or acceptance
of a corrupted world interpretation merely because its aggregate result agreed.

The honest per-world differences are -1, +1, +1, -1. Two controlled corruptions
change individual values while retaining the same aggregate result, including [-1,1].
The existing structural check rejects both. All three new tests and the relevant
geometric/common suites pass, 33 tests in 12.285678 s including process startup.
No core implementation, admission contract or common interface changed. The added
test uses existing fixture and geometric-baseline helpers; it adds no runtime adapter.

The research lane's immutable case at 2ad46bb is an input, not acceptance of its
planner, theorem or correction claims. No Fable source, branch, current status or
outbox is changed. Previous consumer findings remain scoped to their selected source;
later corrections still require a separate immutable consumer review. The Engine's
own `engine-varying-match-2026-09-12` outbox retains exact common inputs, mappings,
certificates and the two rejected projections for optional consumer inspection.

Practical evidence has not advanced in this synthetic test. The prepared native
opening/selection exercise remains the next participant action. The
[desktop diagnosis](PERCEPTION_DESKTOP_RECOVERY_2026-09-12.md) isolates the installed
service's loader incompatibility; no unchanged launch or security-setting change
is justified. A compatible official service/runtime combination is required before
another automated app-state check. No working replacement has been selected.

The real result remains [-8,8], with human reference judgments, actual participant
usability and external scientific review missing. No admission, observation, cohort
or seed is fabricated. The physical study is unrun and Gate A is unaccepted. Joint
alternatives, matching competition, weights, loss, tolerance and unknown states remain
required. P005 is operator-reported published; no further publication or outreach
is authorized. All previous checkouts and closed packets remain preserved.
