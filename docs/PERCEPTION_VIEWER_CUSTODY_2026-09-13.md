# Viewer output custody, 2026-09-13

Document ID: `reiyah.perception-viewer-custody.checkpoint`; version `0.1.0`; status `exploratory`.

The native viewer CLI now requires the location of its previously verified observation package.
It binds that location to the selected seal and capture file, then refuses output inside the
package or source checkout. Eight actual native synthetic preparation/extraction commands
previously contaminated their package; all eight now fail `VIEW_OUTPUT` without source changes.
Private output controls retain exact original point indices. Read the
[changed command contract and reproduction](../research/perception-viewer-custody/0.1.0/README.md)
and [verification](../research/perception-viewer-custody/0.1.0/verification.json).

Binding and selection records remain version 0.1.0, but current coordinator commands must add
`--package`. The guard is standard-library-only and assumes stable directories. It does not
replace complete package verification, authenticate human authorship or establish physical
truth. A matching seal alone is insufficient to verify all contents. The current person-facing
opening/selection/Save As exercise is unchanged; new background outputs are kept separately.

All 20 focused and 369 repository tests pass. The preceding 93 measurement tests are retained,
with all 107 measurement files checked unchanged, rather than claimed as current replay.
On the already selected 34,720-point development capture, native extraction returns exactly
the earlier 1,651-byte report; the selected original indices 0, 17,360 and 34,719 independently
match raw and PLY slices. This is a programmatic source check, with no GUI or human observation.

No frozen protocol, mathematical comparison, reference record or Fable source/ref/status/outbox
changed. The real comparison remains [-8,8]; Gate A is unaccepted, the physical study is unrun,
and human references, actual interactive usability and external scientific review are missing.
The official tool failure remains diagnosed; no unchanged bootstrap was retried. P005 is
operator-reported published; no further publication or outreach is authorized.

Continue from the sealed private `engine-viewer-custody-2026-09-13` packet in a fresh candidate.
Assess whether capture-to-binding preparation can eliminate manual transcription with the
existing verifier/viewer. First count the necessary coordinator steps and challenge incorrect
capture/package selections. Avoid a second viewer or planner. Actual participant return takes
priority; the authorized autonomous Engine window continues.
