# Checked viewer preparation, 2026-09-13

Document ID: `reiyah.perception-viewer-preparation.checkpoint`; version `0.1.0`; status `exploratory`.

A coordinator can now select a capture and derive its existing native-viewer binding from
the complete verified observation package. Asset size, digest and point count are no longer
transcribed manually. The command returns the existing viewer's source argument array, while
package seal and capture choice remain explicit. Read the
[procedure and comparator](../research/perception-viewer-preparation/0.1.0/README.md) and
[verification](../research/perception-viewer-preparation/0.1.0/verification.json).

The binding remains byte-identical to the correct manual baseline. On the retained development
capture, those arguments work with native preparation/extraction and produce exactly the
earlier selection report, preserving all original record indices. Ten new tests cover source
identity, unavailable evidence, modality, output preservation and command behavior. All 379
repository tests pass; the earlier 93 measurement tests are retained with unchanged source,
not replayed. The change adds one small composition module and no new installed dependency.
It leaves the native adapter and 0.1.0 binding/selection records unchanged.

This removes coordinator transcription; it does not measure a human-effort saving. No actual
display, participant selection, discovery or reference interpretation was supplied. The earlier
manual exercise remains ready. The identified desktop loader failure was not retried unchanged.
Actual participant usability, independent human references and external scientific review remain
the decisive missing evidence. No planner or second viewer was built.

A separate read-only consumer review selected Fable's correction at `c70e6ce` through manifest
`1ac89839ed3d0ed5cd5916b330caf9166a2bda64420ed39add4281d84ed550db`.
Fresh version 0.3.0 reports show all six earlier forgeries now refused, both earlier rejection
controls refused, and four honest controls accepted. Full negative-report consumption remains
on hold: a newly constructed witness with value [1,1] and a permitted splitting question is
accepted as undecided and unsplittable. Removing both report identity fields is also accepted.
The exact synthetic case, direct loss calculation, reports and selected source bindings are in
the Engine checkpoint's separate immutable consumer outbox. This finding concerns the selected
bytes only, not later unselected releases. Fable owns any repair; its source, refs and outputs
were untouched. Its shared cohort matcher is not claimed independently verified by this review.

Joint reference alternatives, matching competition, weights, loss, tolerance and explicit
unknowns are unchanged. Real bounds remain [-8,8]; Gate A is unaccepted and the physical study
has no cohort or seed selected. P005 is operator-reported published; no further publication or
outreach is authorized. Preserve all owner checkouts and closed evidence packets. Continue the
autonomous Engine window in a fresh candidate after this checkpoint; participant return takes
priority over further infrastructure.
