# Plan-consumption checkpoint, 2026-09-12

Engine's consumer review confirms the corrected missing-reference behavior in Fable's
immutable `resolution-plan-0.2.0` exchange at `81ba0c6c00a6e6b727aab5719b71c262c341c27e`.
The actual common open operands retain [-8,8], all base/addition identifiers and classes,
joint-reference absence, weights, loss and tolerance. No depth or finite ambiguity is
claimed by the honest producer. Three honest constructed controls and all 34 selected
planner/checker tests pass.
The candidate's 93 measurement tests and research consistency check pass. The unchanged
Engine and native viewer suites remain retained evidence from their earlier checkpoints.

**Broader report consumption remains on hold.** The checker accepts six forged reports:
changed bounds or decision fields, invented witness names, and a false negative-state
explanation. Most directly, the zero-world open case can be given two invented names and
the checker then calls its geometry witness verified. Its response still reports zero
worlds. The release's advertised 0.2.0 interface also differs from its emitted 0.1.0 identity.
Read the [exact cases, reproduction and consumer action](../research/perception-plan-consumption/0.1.0/README.md)
and [verification](../research/perception-plan-consumption/0.1.0/verification.json).

This changes the Engine integration decision, not the separately owned planner. Corrections
remain in Fable's lane; Engine supplies a frozen counterexample packet. No planner or checker
source is copied into Engine runtime, no replacement algorithm is implemented, and no common
operand or admission interface changes. A later correction requires a newly selected immutable
outbox and replay of the discriminating cases. Earlier failures remain discoverable.

The user's viewer exercise has not been returned; the observation form remains unfilled and
the default desktop socket is absent. No unchanged bootstrap was retried. The existing prepared
native scene and opening/selection/Save As procedure remain ready. Actual visible interaction,
participant usability/time, independent human reference judgments and external scientific
review remain missing. None is supplied by this consumer review.

Gate A is unaccepted. The physical study has no selected cohort or seed and has not run.
The real comparison remains [-8,8]; preserve joint alternatives, matching competition, loss,
tolerance, weights and explicit unknowns. P005 is operator-reported published; older notes are
historical and no further publication or outreach is authorized. Both analysts must receive
the same staged evidence with all effort counted. This review leaves the Engine owner checkouts
and closed packets unchanged. The exact private continuation is `engine-plan-consumption-2026-09-12`.
