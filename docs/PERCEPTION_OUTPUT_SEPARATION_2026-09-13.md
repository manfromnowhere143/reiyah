# Output separation across the review workflow, 2026-09-13

Document ID: `reiyah.perception-output-separation.checkpoint`

Version: `0.1.0`

Lifecycle status: `exploratory`

Seven Engine writers now use filesystem identity to keep private outputs out of protected
source directories. Nine case-alias bypasses were reproduced before the correction: six
wrote into existing observations, two into assistance inputs, and one created a disclosure
package containing its own private custody record. All returned success; fresh verification
then rejected the contaminated package. No original file was overwritten.

All 18 direct-path and symlink controls already rejected. All 27 workflow cases now meet their
rejection and preservation expectations. The [reproduction and change](../research/perception-output-separation/0.1.0/README.md)
retain the exact question, comparator, source identities and limits. The
[verification](../research/perception-output-separation/0.1.0/verification.json) records 34 focused
tests, 364 repository tests and 93 measurement tests passing. Existing SQLite warnings remain.

One shared helper replaces repeated lexical policies and is also used by discovery custody.
It compares output-parent ancestors with protected directory identities and returns the resolved
private destination. Actual six-workflow symlink-retargeting controls preserve their source
packages. The new-package builder performs its identity check after creating its directories
and before any payload or custody write. Rejection can retain empty partial directories, without
a completion seal. No whole-filesystem sandbox or concurrent-mount guarantee is claimed.

No record schema, matching semantics, joint alternatives, weights, loss, tolerance, unknown state
or frozen protocol changed. Source identities change because the implementation changed; older
closed packets and their expected bytes remain history. Fable's code, refs, planner, checker and
outboxes are untouched; no later research correction is accepted by this checkpoint.

The next falsifiable question concerns the viewer's file boundary: it receives a package seal
and an asset, but no package location against which to check its outputs. Reproduce a source-
directory mutation before changing that interface. Actual participant return takes priority if
supplied. The existing manual exercise remains ready; interactive observation, human references
and external scientific review remain missing. No unchanged native/browser bootstrap was retried.

Real bounds remain [-8,8]. Gate A is unaccepted. The physical study is unrun with no cohort or
seed selected. P005 is operator-reported published; no further publication or outreach is
authorized. The exact private continuation is `engine-output-separation-2026-09-13`; use a fresh
candidate for the next checkpoint and preserve every older owner checkout and closed packet.
