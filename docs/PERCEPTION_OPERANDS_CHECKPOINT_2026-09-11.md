# Equal analysis operands, 11 September 2026

Question: can both analysts trace every retained detection and normalization disposition to the
same complete source assistance, and evaluate the same declared comparison?

Yes for the present open-reference stage. Two corrected private preparations are byte-identical.
A separately structured conventional calculation, importing no Reiyah code, confirms every
comparison-row disposition and retained detection. The existing producer and separate certificate
checker accept the neutral comparison and reproduce the unchanged open-reference bound [-8,8].
This checks representation and arithmetic; it supplies no new physical reference evidence.

The unchanged two previously exposed anchors contain 648 source prediction rows: 490 are
ineligible, 158 qualify, 85 base detections survive and 16 candidate detections are retained.
The remaining 57 qualified candidate rows are suppressed under the existing rule. Every row
remains in the common assistance. The 16 source keyframes, including 960 annotations and 4,876
predictions, remain inspection context; they do not become 16 loss opportunities. The original
725-capture observation package is unchanged.

The [interface](../research/perception-operands/0.1.0/README.md) binds an explicitly expected
assistance preparation to a separately expected comparison/observation request. It freshly checks
population and delivered assets, restores retained source rows, checks each frame's state/count/
sample correspondence, and reruns normalization at the comparison anchors. It preserves duplicate
row identity, complete eligibility/suppression traces, exact geometry, source availability, loss,
weights and tolerance. Structured source IDs stay in operator custody. Substantive free-text
assumptions and reasons remain visible to both analysts; this private packet is not anonymous and
does not establish blinding.

The [analyst instructions](../research/perception-operands/0.1.0/ANALYST.md) and
[rules](../research/perception-operands/0.1.0/rules.json) expose the same permitted mathematics and
inputs to both methods. This is conventional projection and accounting. The independent
calculation checks the same score/range/suppression rules directly; it omits source preparation
and asset binding, so its shorter runtime is not a speed comparison. Exact process costs and
source identities are in the [machine checkpoint](../research/perception-operands/0.1.0/checkpoint.json).
Person preparation, review and integration effort remains unmeasured, not zero.

All 321 repository tests and 93 measurement tests pass, including 12 new operand tests. Internal
review found that a context frame could contradict its bound descriptor while the comparison
anchors still matched. A resealed missing-to-observed-empty example reproduced the defect.
Three failing regression mutations were retained before the correction checked every context
state, count and sample. A subsequent summary correction preserves unavailable totals explicitly.
Both earlier outputs and corrections remain private retained history. This is internal engineering
review, not independent external scientific validation.

This version refuses finite references and nonempty joint models. It does not strip reviewed
constraints, manufacture an empty reference or admit source annotations. Both analysts will need
the same later admitted interpretations and their exact detection mappings before a reviewed
comparison. A different research-lane population, reference filter or disputed-set rule cannot
supply those interpretations by substitution.

The next useful empirical step requires competent independent discovery reviewers, conventional
analysis, adjudication, preserved exposure order and a practical viewing workflow. No reviewer
has supplied a judgment in this checkpoint. No assistance was released, cohort selected, seed
chosen or physical study run. Gate A remains unaccepted. Offline Engine work and professional
main integration retain their separate operator authorization.
