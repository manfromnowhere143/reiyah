# Conditional continuous clearance, version 0.1.0

Status: exploratory authored development study. Research cutoff: 20 September
2026. No physical data are selected. The eight closed NGSIM windows and all
1,433 reserved images remain unopened in this increment.

The preceding adapter cannot establish between-sample clearance without a
motion and measurement contract. This increment makes that missing contract
computable. Given interval observations of one signed road-projected clearance,
a supplied Lipschitz bound and one shared clock offset, compute the exact least
and greatest achievable minimum over a closed horizon. Retain feasible witnesses,
inconsistent premises and missing assumptions. Do not infer bounds from a
dataset's approximate accuracy or maximum observed speed.

The equally informed conventional reference solves small piecewise linear
programs by exact vertex enumeration. The producer uses cone intersections.
They share format/custody code only. Neither is an independently authored
workflow or external replication. Parity supplies correctness evidence, not
comparative value. No novel mathematical primitive, runtime control, generalized
STL engine, probabilistic coverage or latency advantage is claimed.

Freeze code, authored cases, method, source ledger, control allocation, runtime
and supervisor before the allocated run. Never replace a failed case. A changed
frozen file requires a retained correction and new binding. Run all allocated
cases, a finite parameter grid, interval-expansion relations, critical malformed
inputs, forged outputs and custody mutations. Record exact outputs and failures.
Allocation: 20 named authored cases, twelve hand-derived expectations, all 972
two-observation combinations in controls.py, and three uniform expansions of
each feasible grid case. Check 26 malformed-input families, fifteen forged
results, four strict boundaries and one disabled-producer check. Integrity and
supervisor probes use fresh owned copies; no historical study is rerun.
Both support and contradiction require nonempty feasible worlds; equality at the
threshold satisfies the non-strict requirement. Missing is never zero.

Resources: 128 MiB new artifacts including owned temporary files, checkout
separately counted; 8 MiB captured sources; 5 GiB free-space floor; cumulative
600 seconds execution and 600 seconds checking, including failed attempts.
These are ceilings, not target durations. Offline children enter macOS network
denial before Python starts. Source/push/readback are explicit online stages.
Point resource samples do not bound unobserved peaks. Engineering effort,
charges, energy and complete memory remain unmeasured unless actually recorded.

Closeout includes bounded controls, current research consistency (historical
transcripts exact-bound, not replayed), documentation and diagram preservation,
full diff and human Git identity review, normal guarded push, exact publisher
readback, final cost ledger and continuation checkpoint. Gate A stays
operator-unaccepted. No other model or sibling repository is a dependency.
