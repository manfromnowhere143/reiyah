# Measured work and unmeasured economics

Document ID: `reiyah.sequential-audit.costs-note`.
Version: `0.1.0`.
Lifecycle status: `exploratory`.

The controlled outcome producer used **1.470357750 seconds** of outer-process
elapsed time; the separate actual-result checker used **2.139591000 seconds**.
Each is below its own 300-second cumulative ceiling. Neither failed nor capped.
Single-worker child user/system CPU times are retained separately in the private
command receipts. Query counts and these exhaustive-enumeration times are
different quantities and must not be substituted for reviewer time.

The public costs.json snapshot includes 14 recorded setup Git commands, fixture
generation, the actual producer and checker, and two control commands. The
control commands contain two expected rejected tampered copies; these are
successful rejection tests, not failed scientific runs. Their subprocess times
are inside the enclosing control timers. Per-population/arm enumeration times
are likewise inside the producer timer and are not added again.

At the snapshot, private artifacts plus the public packet occupied 7,499,628
bytes, below 64 MiB. The inherited candidate checkout is a separate cost.
Three primary works reuse eight exact-bound response bodies already retained
by the earlier audit. No new paper body, data, image, model or dependency was
downloaded. Their prior retrieval cost is not charged again as new work.

First known instrumentation is 19 September 2026 at 17:02:18 UTC; the exact
user-turn start is unknown. Many reads, web-tool calls, source interpretation,
authoring and reasoning are not included in the measured process intervals.
The initial combined discovery/read tool reported 8.9 seconds without an owned
UTC interval; it is retained separately, not added to the interval union.
This is not complete active engineering effort or complete session billing.

The accountant, final consistency and public-scope checks, commit, integration,
publisher readback and closeout occur after the public snapshot. Their owned
receipts and the final accounting supplement remain in the closed private packet.
The final supplement preserves its own explicit cutoff and avoids nested counting.

Human/useful effort, acquisition/adjudication costs, full workflow costs and
monetary value remain unknown. No economic saving, demand or valuation follows
from these authored query counts. All negative and unresolved results remain.
