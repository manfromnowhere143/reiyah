# Scale-study consumer evidence

Version: `0.1.0`. Evidence kind: synthetic counterexample and sealed-result review.

[Result and source bindings](result.json), [exact Engine case](case.json),
[bounded replay](replay.py), [census arithmetic and conditional bounds](census-summary-check.json), [interpretation and limits](../../../docs/PERCEPTION_SCALE_CONSUMER_2026-09-15.md).

Run from the repository with the selected read-only Python runtime:

```text
python -B research/perception-scale-consumer/0.1.0/replay.py --fable-module <sealed tools__measure__scale_study.py>
```

The replay requires module SHA-256
`a263ef8661d2169c710e79848145cc4145033d2a23631c0ab58e49cc3f67fbd6` before importing it.
The private selected source is retained in
`~/.codex/reports/reiyah/engine-scale-consumer-2026-09-15-tmw4ob4q/inputs/scale-pilot-0.1.0/`.
It is also Git blob `6b5989a72b91d0f2111c365d525bef9b899fb7ca:tools/measure/scale_study.py`.
The census commit `10ed423b479384b2160e39bcff4528c79c23c3bf` contains the identical module.
Its separately verified outbox is retained in the same checkpoint under `inputs/scale-census-0.2.0/`.
This source is a reviewed local Python module, not sandboxed untrusted execution.

Expected: eight exact subset agreements, floor one, minimum two, the retained
research search's false `no_admissible_crossing`, a silently removed missing
frame and four malformed numeric rows retained by its qualifier. The Engine
preserves missing input as `input_blocked`. A future corrected research module
requires a new selected artifact, not replacement of this retained source.

The brute enumerator shares neither matcher. The Engine kernel generates proofs
and its existing checker verifies matching/cover certificates. They still share
the input schema and exact-rational representation. Actual pilot source tables,
selected deletion members, physical labels, human effort and customer value are
not verified by this replay. It does not implement a new optimizer or comparator.
