# Common-world construction and checking

Version `0.1.0`. The [plan](PLAN.md) was frozen before new search outcomes.

Known modeled references K remain fixed. Each declared unknown instance can
contribute one eligible rectangle or be absent/ineligible. Duplicated geometry
does not merge instance identities. For the same reference world T, maximum
one-to-one matching gives the inherited unit-loss difference
`D(T) = |A|-|B|+2*(m(B,T)-m(A,T))`. Cases sum image differences and divide by
their full allocated population. Strict improvement means D>0; D=0 is
non-improvement. An unavailable union census blocks its complete case.

The thin adapter reuses `witness_pool.build_pool` and `witness_search.search`
unchanged from [reference-witness](../../reference-witness/0.1.0/PLAN.md).
Its declared bases include filtered predictions, K, the source-bound original
coarse pool, and optional geometry in the inherited temporal world bank. Adding
the last set preserves existing seed neighborhoods after filtering. Both paths
reconstruct that exact base allocation; no newly acquired geometry is used.

Exact rational IoU transitions partition legal one-axis translations of each
base. Boundary points and interval midpoints induce common neighborhoods against
both detector outputs. The first geometry in lexicographic order represents
each nonempty neighborhood. Empty neighborhoods do not affect matching ranks.
Multisets of representatives preserve matching competition and multiplicity.
Breadth-first lexicographic search permits repeated indices and zero-gain
prefixes. Its inherited descendant enclosure prunes only when neither incumbent
can improve. Seeds and distinct tuple measurements count against the same cap.

Pool reuse binds the full filtered image, known geometry and base set. Search
reuse additionally binds ordered unknown identities, the exact pool, seed
measurements and unchanged bounds. Eleven of the 100 searched contract pairs
reuse an earlier identical context. All 89 charged searches reach both bounds;
their exact extrema follow from sound universal enclosures and attained common
worlds. Neither finite-pool exhaustion nor equal tested scores would suffice.

The checker reconstructs the allowed optional operations and every omitted
instance, validates geometry/eligibility and preserves the known prefix. A
separate exact-IoU and augmenting-path route recomputes matching. Native checking
binds graph bytes, payload and full prediction/reference operands. Selected
endpoints must strictly improve an incumbent; ties retain the inherited proof.
Unselected search endpoints remain in the private proof inventory.

Independent piecewise-overlap code reconstructs every declared pool. Traversal
replay shares the inherited search implementation and verifies its charged
accounting; it is not independent evidence of general completeness. Ninety-six
tiny exhaustive multiset controls check pruning/extrema, alongside narrow
threshold, repeated-box, non-improving-prefix, zero-budget and resource controls.
Adapter controls reject forged operations, wrong census/budget, absent inputs,
changed threshold context or bounds, invalid reuse and altered native payloads.

The separate operating case checker recomposes all 7,707 rows. It verifies
membership, cutoff identity, blocked rows, universal intervals, strict decisions
and exact proof sums. This produces 279 new opposing-world classifications and
282 nontarget interval refinements through shared states. Existing native proofs
are exact-bound to their earlier completed checks; this is explicit reuse of
internal evidence, not another independent verification of those proofs.

The separate publication checker compares every CSV field with the retained
checked result, including rational anchor thresholds and explicit missing cells.
It also checks aggregate counters and source/timer bindings. Frozen scientific
implementation and source packets remain unchanged throughout packaging.
