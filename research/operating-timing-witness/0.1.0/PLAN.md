# Refine every retained operating timing gap

Version `0.1.0`. Exploratory, outcome-informed development follow-up. Parent
outcomes are known. No prospective, held-out, human-review or replication claim.

Select exactly the final operating-witness table's 279 bound-gap rows: 224
primary and 55 diagnostic anchors. Preserve all 7,707 parent rows. Their union
contains 356 filtered image states and 397 state/partial-contract pairs (356
current, 41 union). Of those pairs, 297 already attain both universal bounds;
100 need optional-reference search. Retain every pair, including nonsearch
pairs. These are dependent operands, not independent observations.

Freeze these identities, all consumed source/proof/check bytes, implementation,
controls, pinned runtime and supervisor before new actual searches. Original
freezes and historical controller remain unchanged. Keep score cells, image
membership, prediction geometry, fixed modeled references K, unknown-instance
censuses, eligibility, exact rational IoU >= 1/2, unit penalties and all
universal bounds unchanged. Unavailable censuses remain unavailable.

Use the unchanged reference-witness pool and breadth-first multiset search.
Bases comprise the filtered predictions, known references, original temporal
coarse-pool rectangles, and optional rectangles from the inherited temporal
world bank. The last set explicitly preserves seed neighborhoods under the
new filtered graph; it is an extension of base allocation, not of the geometry
solver. Sweep each base along each axis across the legal canvas using exact
IoU-transition points and interval midpoints. Keep the lexicographically first
geometry per common nonempty neighborhood. Empty neighborhoods may be omitted
without changing matching. This is not exhaustive four-dimensional geometry.

For each nonexact pair in sorted (state digest, family) order, seed with both
inherited endpoints after neighborhood mapping. The known-only world is also
measured. Run unchanged breadth-first lexicographic multisets, allowing repeated
neighborhoods for distinct unknown instances and non-improving prefixes. Both
detectors use the same constructed world. Loss difference is
`D = |A|-|B|+2*(m(B,T)-m(A,T))`. Keep inherited endpoints on ties or regression.
Retain every new endpoint, its operations and native proof, even if unselected.

Reuse a pool only for an identical filtered state and exact base/known context.
Reuse a search only when full image, known references, ordered unknown IDs,
pool, seed measurements and universal bounds agree. Record the supplying pair
and charge its search once. Similarity, IDs alone or adjacent cutoffs are not
reuse authority. All untargeted families stay unchanged; report every nontarget
case refinement caused by shared selected states.

Limits per search: 100,000 unique rectangles, 4,096 nonempty neighborhoods,
50,000 distinct flow measurements including seeds, 128 known-plus-optional
references per native proof. Pool overflow is a retained incomplete construction,
not truncated uncertainty. Finite exhaustion does not prove universal equality.
Only verified attainment of both unchanged bounds proves exact extrema.

The append-only supervisor caps cumulative new actual search process wall time
at 1,200 seconds and actual verification process wall time at 1,200 seconds,
including source checks, proof construction/checking, composition, failed and
retried attempts and output within those processes. Never add nested timers to
outer totals. It reserves termination headroom; the search receives an earlier
deadline to retain completed records and mark the remaining allocation capped.
Controls/setup are separate. At most 1 GiB of new research artifacts including
temporary controls/proofs; account for the fresh checkout separately. Stop
growth before filesystem availability is below 5 GiB. No new environment,
dependency/model download, inference, image/label/metadata acquisition or reserve
access. All 1,433 reserved images remain closed.

Before actual search run inherited narrow-cell, repeated-neighborhood,
non-improving-prefix, 96 tiny exhaustive multiset, zero-budget, seed, reference
and pool-cap controls. Add adapter controls for context/identity reuse, optional
operations, missing inputs, wrong census/budget, wrong threshold, changed bound,
proof mutation and complete-case arithmetic. The separate checker independently
reconstructs exact axis cells, neighborhoods, allowed worlds and augmenting-path
matching; checks native graph/payload bindings and every parent case through
the existing separate case checker. Shared traversal replay checks accounting,
not independent completeness. Bind inherited proofs to their prior checks;
do not recharge old computation or call reuse replication.

Retain all failed/incomplete attempts and inherited valid evidence. A witness
outside an inherited universal bound stops the affected claim and requires an
explicit corrected freeze; never rewrite history. Every target is classified
as opposing worlds, remaining bound gap, or specific incomplete search retaining
its prior evidence. Sum over each complete case and divide by its full population.
Strict improvement is D>0; zero is non-improvement. Endpoint attainment of a
universal bound is not necessary to establish opposing worlds.

Publish reviewed authored code, synthetic controls and derived summaries only,
with inherited source terms and attribution. Keep raw inputs, world geometry
and actual proofs private. Recompose and check all rows, run the current research
consistency check under pinned Python and network denial, inspect scope, integrate
with guarded ancestry/ref updates and normal safe push, and read back every
changed public artifact. Publisher integrity is not independent transport or
scientific verification. Preserve the completed old clock, excluded interval,
failures, storage recovery and CSV correction. Record new costs separately;
human/full economics, demand and savings remain unknown. Do not automatically
start another sensitivity study when this bounded question finishes.
