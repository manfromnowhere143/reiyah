# Frozen development protocol, 0.1.0

This protocol is frozen before this packet's numerical controls and workflows.
The earlier continuous draft was never frozen or executed; its exact private
files and supersession rationale remain retained.

Question: does the implementation compute exact recorded-reconstruction fidelity
bounds and complete encoded reply classes, with a competent conventional method
given exactly the same native grid, encoding, pins, rate, tolerance and costs?
This is a development correction to known over-relaxation. All five source
parts were exposed in navigation-measurement 0.2.0. They are dependent portions
of a single one-second recording. They are not new held-out observations.

Use all 13 authored cases in cases.py and all five fixed exposed tail parts.
Keep the source selection, source bytes, 5 m/s² rate premise, 0.05 m/s tolerance,
coarse indices [0,10,20] and native spacing unchanged. Do not inspect further
source slices, tune parameters, remove adverse parts or reinterpret physical
accuracy. The tolerance remains an authored numerical example, without a
customer or safety requirement. No physical case is executed.

The earlier five-part result remains Reiyah 90 versus conventional 89 logical
reveals. Do not overwrite it. It used different relaxed families. A changed count
here may expose that modeling cost, but is not a fair claim that the new method
beats the old conventional method. The current comparator receives all new
constraints too. Preserve parity, disadvantages, failures and unresolved results.

1. Verify exact code/protocol/schema/source/runtime/supervisor freeze.
2. Run exhaustive small-domain controls and strict malformed/source refusals.
3. Execute Reiyah once, then conventional once, in separate measured processes.
   Both execute identical case order and equally detailed partitions/witnesses.
4. Independently replay every workflow; bind source bytes and the already checked
   manufacturer-decoder lineage. Check direct raw encoding/clock for 101 selected
   values and packet integrity for the 1,000-packet slice.
5. Apply twelve predeclared mutations to actual retained output. A failed control
   is a failure, not a reason to weaken its required refusal. Any frozen code
   correction receives a new version with old bytes and failed records retained.
6. Publish only authored implementation, aggregate results, identities, failures
   and cost records. Raw recording, decoded rows, full trajectories, full workflow
   traces, third-party papers and rendered paper pages remain private.

The finite control grid has two time grids; all 3x3 endpoint pairs, three rates
and three tolerances; and three extra interior-pinned half-unit cases. It uses
directly enumerated finite worlds. These are authored correctness controls,
not empirical evidence or external scientific replication.

Known costs distinguish whole supervised processes, internal equal-scope arm
workflows, source acquisition/reading, checking, packaging and push/readback.
Full input is already loaded; logical reveals do not measure hardware acquisition
savings. One fixed-order run cannot establish general speed superiority.
Human/agent engineering effort, charges, energy, network overhead and unsampled
memory/storage peaks stay unknown. No active-effort duration is fabricated.

Caps inherited by the fresh owner: 8 MiB new source/derived bodies, 128 MiB owned
artifacts, 5 GiB minimum free disk, 600 execution and 600 checking process seconds;
failures count. Checkout is accounted separately. Offline child isolation starts
before its runtime. Historical controller and all 1,433 reserved images remain
untouched. No training, inference, private-team records, deployment, physical
control, paid compute, new dependencies or outreach.

This is research replay, not Gate A release evidence or operator acceptance.
After scoped repository checks, normal guarded integration/push and publisher
readback are already authorized. The continuing mission remains ACTIVE after
this study is closed. Next scientific evidence needs a separately frozen useful
decision on unexposed qualified material; development success cannot substitute.

