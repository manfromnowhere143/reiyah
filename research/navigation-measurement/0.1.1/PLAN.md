# Navigation measurement allocation

Document: reiyah.navigation-measurement.plan, version 0.1.1.
Status: exploratory recorded-data development. Date: 21 September 2026.

The immutable QUESTION.md and question-freeze.json fix the source prefix,
selector, one-second recorded-data decision, tolerance and original rate premise.
The subsequently specified METHOD.md strengthens the conventional comparator
before numeric outcome inspection: it retains the original velocity family,
while Reiyah uses its declared residual relaxation. This is not an equal-bounds
comparison or a requirement that verdicts and query counts tie. Both receive the
same original assumptions, coarse values and available measurements. Preserve
any conventional advantage. The original question bytes remain unchanged.

Freeze the implementations, this plan, method, question, source identities,
lineage, supervisor, runtime and compiled manufacturer-decoder wrapper before
executing the allocated scientific calculation. No numeric navigation outcomes
have been decoded during access and source-code preparation. The public example
is development material, not a reserved, blinded or independently selected test.
Five adjacent segments share endpoints and belong to one supplied example.

Allocation: 72,000 source bytes, 1,000 encoded packet positions; first eligible
101-sample run only; five adjacent 21-sample/20-interval segments; three initial
coarse samples per segment; at most 18 additional measurements per segment and
90 per arm. The eleven unique coarse samples are shared at boundaries. Reused
boundary knots do not become independent observations or physical cases.

Execute exactly the 25 named pre-controls in controls.py after freezing. They
cover source parsing, unavailable clocks, explicit invalid values, checksums,
clock rollover, population selection, quantity units, the 21-observation cap,
continuous two-sided response partitions and strict/equality boundaries, the
strong comparator's slope information, query reuse and source/freeze tampering.
Then run the Reiyah arm, the separate numerical/witness check, manufacturer input
calculation, original-coordinate comparator and full recorded-data reference.
Finally run the eight declared actual-result mutations if a source case executes.
A blocked source selection is retained and does not authorize a replacement
window; its unavailable post-result mutations are not counted as passes.

Retain every actual response, prior/updated decision bounds, complete response
partition, witness stage, query identity, source mapping, invalid record and
unknown. Keep full source-derived traces privately. Test the original rate
premise against all 100 selected native intervals. Rejected premises invalidate
the empirical binding of the conditional conclusion; do not retune or hide them.
Empty empirical error lists are scoped to applicable recorded reference cases.

Costs include source discovery/capture, range probes, decompression, decoding,
compilation, acquisition/preparation, both calculations, independent checking,
controls, packaging, integration and readback. Charge the complete prefix even
if logical queries stop early. Record nested timers without adding them twice.
Active effort, charges, energy, peak memory and complete economics stay unknown.
Point resource samples do not bound unobserved peaks. Continue existing 8 MiB
source, 128 MiB artifact/temp, 5 GiB free-floor and 600 s execution/checking caps.

Use only the existing pinned Python and local compiler. Execution/checking starts
with network denial before child runtimes. No external model, sensor, inference,
training, new dependency, paid compute, outreach, deployment or physical control.
All 1,433 reserved images, prior failed findings, closed studies, historical
controller, perception kernel and six README diagrams remain preserved. The
continuing mission stays active across integration of this study checkpoint.
