# Retained operating-policy witness gaps

Version `0.1.0`. Status `exploratory`. The preceding operating-policy outcomes
are known. This disclosed follow-up refines evidence without changing the
allocation, reference family, universal bounds, decision rule or thresholds.

Allocate every preceding `bound_gap` row in the one-edit-global or
one-edit-per-image family: 110 rows, including 10 primary continuum rows and
100 diagnostic-anchor rows. Their union contains 185 filtered image states;
98 have a one-edit attained endpoint different from its universal bound.
Keep all 185 states, including the 87 already exact. Retain all 7,707 parent
case rows, including every input-blocked row and the other reference families.
Any improvement on a nontarget row through a shared state is disclosed.

Reuse the original finite geometric edit search on each of the 98 filtered
states, with its original projected references. The prior operating-policy
bank had retained original extrema and all deletion worlds; it had not
rerun the full candidate construction at each score threshold. Candidate
shapes, exact one-axis IoU cells, arbitrary eligible insertion/deletion/
replacement semantics and conventional matching are inherited unchanged.
This is a finite witness search, not exhaustive arbitrary-rectangle geometry.
An attained universal endpoint is exact by its previously verified bound.

Freeze parent result and verification, all input state/proof/bank bytes,
inherited implementation dependencies and this implementation before the
new search. Cap at the 185 declared states, 100,000 candidate rectangles
and 2,000,000 neighborhood/removal pairs per searched state, 370 new endpoint
proofs, and 1,200 seconds for each analysis/check process. No images, new
predictions, downloads, inference, training, reserve access or paid compute.

Retain each completed search or failure. On failure retain the parent state
and bounds with an explicit search-incomplete record. Never infer a world
from a bound, relabel a failed search as an empty input, or discard a row.
For each attained improvement retain the full edited world, exact conventional
matching, native proof and computation/check timers. Compose case witnesses
with the unchanged one-edit-per-image/global rules and equal caches.

Verify new edits, geometry, membership and each new proof using separate
matching and existing certificate checking. Check every case's arithmetic
through the separate certificate path; bind inherited proofs to their prior
verified bytes without charging their old checks again. Reject forged edits,
changed bounds, changed membership, unavailable inputs and altered proof
payloads. Include full synthetic filtered-state and case composition controls.

Report both the target-row effect and every row's final evidence state. The
timing contracts' other gaps are outside this edit search and remain visible.
Neither a smaller bound-gap count nor an opposing world is observation savings,
independent replication, a customer revision, or a new operating recommendation.
All 1,433 reserved images remain closed. Human/full costs remain unknown.
